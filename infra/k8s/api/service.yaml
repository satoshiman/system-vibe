apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: system-vibe
  annotations:
    # GKE Ingress health check config
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: api
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
